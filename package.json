{
  "name": "openclaw-task-mcp",
  "version": "0.1.0",
  "description": "MCP server over Streamable HTTP for OpenClaw task lifecycle",
  "type": "module",
  "engines": {
    "node": ">=18.19.1"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.5",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
